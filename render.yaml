services:
  - type: web
    name: omr-scanner
    env: python
    buildCommand: |
      cd frontend && npm ci && npx vite build && cd .. && pip install -r backend/requirements.txt && mkdir -p backend/uploads backend/results
    startCommand: "cd backend && python app.py"
    envVars:
      - key: PORT
        value: 10000